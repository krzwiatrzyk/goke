version: 3

includes:
  dapr: 
    taskfile: dapr/Taskfile.yaml
    dir: dapr
  devbox: devbox/Taskfile.yaml
  httpie: httpie/Taskfile.yaml
  grpc: grpc/Taskfile.yaml

tasks:
  install:tilt:
  install:vendir: